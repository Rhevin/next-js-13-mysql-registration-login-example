# Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=next-js-registration-login-example

# JWT
JWT_SECRET=replace-with-a-strong-random-secret

# API (public — accessible in the browser)
NEXT_PUBLIC_API_URL=http://localhost:3000/api
